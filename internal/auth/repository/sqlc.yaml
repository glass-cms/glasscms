version: "2"

sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "query"
        out: "query"
        emit_prepared_queries: true
        emit_db_tags: true 